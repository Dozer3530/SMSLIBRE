# SMSLIBRE runtime dependencies (Linux-native; all have manylinux wheels).
# Install into a venv:  python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
#
# On Linux Mint you can alternatively use distro packages
# (python3-geopandas python3-pyside6 python3-matplotlib), but pip in a venv is
# the most reproducible.

geopandas>=1.0
pyogrio>=0.9        # shapefile/vector I/O engine used by geopandas
shapely>=2.0
pyproj>=3.6
numpy>=1.26
pandas>=2.2
matplotlib>=3.8
PySide6>=6.6        # Qt bindings for the viewer
